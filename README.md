# 🧩 สิ่งที่ต้องมีใน environment ใหม่

| ลำดับ | รายการ                                           | หมายเหตุ                                                                                                                |
| ----- | ------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------- |
| 1️⃣   | **Docker Desktop** ติดตั้งเรียบร้อย              | ต้องเปิด “Enable Kubernetes” ใน Settings → Kubernetes                                                                   |
| 2️⃣   | **Kubernetes cluster (docker-desktop)** เปิดขึ้น | พิมพ์ `kubectl get nodes` ต้องขึ้นว่า `docker-desktop Ready`                                                            |
| 3️⃣   | **ไฟล์ manifests / Helm chart / ArgoCD config**  | มีอยู่ใน repo GitHub ของคุณอยู่แล้ว (ไม่หายแน่นอน 🎯)                                                                   |
| 4️⃣   | **ArgoCD ติดตั้งใหม่ใน cluster**                 | ใช้คำสั่ง `kubectl create namespace argocd`  `kubectl apply -n argocd -f https://raw.githubusercontent.com/argoproj/argo-cd/stable/manifests/install.yaml` |
| 5️⃣   | **เชื่อม ArgoCD เข้ากับ repo เดิมของคุณ**        | ArgoCD จะ auto-sync Helm chart ได้ทันที (pull จาก GitHub)                                                               |
