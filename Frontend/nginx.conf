server {
    listen 80;
    server_name localhost;

    root /usr/share/nginx/html;

    location / {
        index index.html;
        try_files $uri /index.html;
    }

    # proxy ไป backend
    location /api/ {
        proxy_pass http://backend:8080;
    }
}
